Given two singly linked lists which intersect at some node. Find and return the node. Note: the lists are non-cyclical.

Example:

A = 1 -> 2 -> 3 -> 4
B = 6 -> 3 -> 4

This should return a list starting at 3.
